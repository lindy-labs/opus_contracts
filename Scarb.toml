[package]
name = "aura"
version = "0.1.0"

# Build each contract as a standalone JSON file
[[target.starknet-contract]]

[scripts]

test_utils = "cairo-test --starknet . --filter utils"
test_shrine1 = "cairo-test --starknet . --filter shrine"
test_shrine2 = "sed 's,mod test,//mod test,g' -i src/tests/shrine.cairo; echo 'mod test_shrine_compound;' >> src/tests/shrine.cairo; cairo-test --starknet . --filter compound"

[dependencies]
