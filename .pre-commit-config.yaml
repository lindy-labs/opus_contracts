# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks

repos:
  - repo: local
    hooks:
    - id: cairo-format
      name: cairo-format
      language: system
      entry: scarb fmt --workspace
      pass_filenames: false
